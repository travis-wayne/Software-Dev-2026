{
  "name": "lambda-serverless-demo",
  "version": "1.0.0",
  "description": "Lesson 42 — AWS Lambda Serverless Handlers & Local API Gateway Runtime Simulator",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "postinstall": "node -e \"import('fs').then(fs => !fs.existsSync('.env') && fs.copyFileSync('.env.example', '.env'));\"",
    "dev": "node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
