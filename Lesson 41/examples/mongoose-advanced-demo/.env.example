# Lesson 41 — MongoDB Configuration Example
# If MONGODB_URI is left blank or commented out, the server will automatically spin up an in-memory MongoDB server (mongodb-memory-server) for zero-setup local testing!
# To connect to MongoDB Atlas or a local Docker instance, uncomment below:

# MONGODB_URI="mongodb+srv://user:pass@cluster0.mongodb.net/lesson41_advanced_odm?retryWrites=true&w=majority"
PORT=3002
