{
  "name": "mongoose-advanced-demo",
  "version": "1.0.0",
  "description": "Lesson 41 — Mongoose Advanced ODM Demo (Virtuals, Middleware Hooks, Populate, Memory Server)",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "postinstall": "node -e \"import('fs').then(fs => !fs.existsSync('.env') && fs.copyFileSync('.env.example', '.env'));\"",
    "dev": "node src/server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.0"
  },
  "devDependencies": {
    "mongodb-memory-server": "^9.1.8"
  }
}
